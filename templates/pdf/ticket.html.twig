<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Event Ticket</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #333;
            padding: 30px;
            background-color: #fff;
        }

        .ticket-container {
            border: 2px dashed #444;
            padding: 20px;
            max-width: 700px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            margin: 0;
            font-size: 28px;
            color: #007BFF;
        }

        .section {
            margin-bottom: 20px;
        }

        .label {
            font-weight: bold;
            margin-right: 5px;
        }

        .footer {
            text-align: center;
            font-size: 12px;
            color: #888;
            border-top: 1px solid #ccc;
            padding-top: 10px;
            margin-top: 30px;
        }

        .qr {
            text-align: center;
            margin-top: 30px;
        }

        .highlight {
            background-color: #f8f9fa;
            padding: 10px;
            border-left: 5px solid #007BFF;
        }
    </style>
</head>
<body>
    <div class="ticket-container">
        <div class="header">
            <h1>{{ event.title|upper }}</h1>
            <p>{{ event.location }} | {{ event.startDate|date('Y-m-d H:i') }}</p>
        </div>

        <div class="section">
            <p><span class="label">Attendee:</span>
            {{ booking.user.userProfile ? booking.user.userProfile.fullName : booking.user.email }}</p>

            <p><span class="label">Email:</span> {{ booking.user.email }}</p>

            <p><span class="label">Booking ID:</span> {{ booking.id }}</p>
        </div>

        <div class="section highlight">
            <p><span class="label">Ticket Type:</span> {{ booking.ticketType.name }}</p>
            <p><span class="label">Quantity:</span> {{ booking.quantity }}</p>
            <p><span class="label">Price Per Ticket:</span> {{ booking.ticketType.price }} INR</p>
            <p><span class="label">Total Price:</span> {{ booking.ticketType.price * booking.quantity }}    INR</p>
        </div>

        <div class="qr">
            <p>Scan this at entry</p>
            {# Optional: Insert a QR Code image here if implemented later #}
            <img src="https://api.qrserver.com/v1/create-qr-code/?data=Booking:{{ booking.id }}&size=100x100" alt="QR Code">
        </div>

        <div class="footer">
            <p>This ticket is valid only for the registered user. Bring a valid ID and this printed ticket to the event.</p>
            <p>Thank you for booking with {{ event.organizer.userProfile.fullName ?? 'our platform' }}</p>
        </div>
    </div>
</body>
</html>
