<form method="post" action="{{ path('app_organizer_profile_delete', {'id': organizer_profile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organizer_profile.id) }}">
    <button class="btn">Delete</button>
</form>
